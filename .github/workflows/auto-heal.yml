name: Auto Heal Test Workflow

on:
  push:
  pull_request:

jobs:
  auto-heal:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ISSUE 1: Wrong command (will fail)
      - name: Run auto heal script
        run: |
          echho "Running auto heal"

      # ISSUE 2: Missing required permissions (logic issue)
      - name: Try to write file
        run: |
          echo "test" > test.txt
